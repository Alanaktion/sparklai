{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "User Profile",
  "description": "Schema for a user profile with various attributes.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The user's name."
    },
    "pronouns": {
      "type": "string",
      "description": "The user's preferred pronouns.",
      "enum": [
        "he/him",
        "she/her",
        "they/them",
        "he/they",
        "she/they"
      ]
    },
    "appearance": {
      "type": "string",
      "description": "A comma-separated list of keywords to describe the user's appearance. Include keywords to identify visual traits like gender style, colors, etc."
    },
    "bio": {
      "type": "string",
      "description": "A short biography about the user."
    },
    "writing_style": {
      "type": "string",
      "description": "How the user writes - brief messages, ."
    },
    "example_post_body": {
      "type": "string",
      "description": "An example post body for the user."
    }
  },
  "required": [
    "name",
    "pronouns",
    "appearance",
    "bio",
    "writing_style",
    "example_post_body"
  ]
}
