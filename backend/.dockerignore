# Applies to Dockerfile.dev, whose build context is backend/ (the root .dockerignore only
# covers root-context builds, e.g. the main Dockerfile).
.venv
__pycache__
.pytest_cache
.ruff_cache
*.egg-info
.env
.env.*
!.env.example
